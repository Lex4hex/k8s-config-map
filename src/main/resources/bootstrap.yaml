spring:
  application:
    name: configmap-test-app
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        paths: "/deployments/config"