spring:
  application:
    name: configmap-test-app

---

spring:
  profiles: spring-cloud-cubernetes-profile
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        name: example-reloadable-configmap

---

spring:
  profiles: volume-config-profile
  cloud:
    kubernetes:
      config:
        paths: "/config-volume/"